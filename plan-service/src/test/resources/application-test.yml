spring:
  datasource:
    url: jdbc:h2:mem:gymai_plan_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false

jwt:
  secret: test-jwt-secret-that-is-long-enough-for-hmac-signing-32-bytes
  expiration: 900000

app:
  environment: test

cors:
  allowed-origins:
    - http://localhost:4200
